export function dashboardHtml(): string {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>AntroSwarm Control Center</title>
<style>
:root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#070a10;color:#edf2fb;--bg:#070a10;--panel:#0d121c;--panel2:#111824;--border:#253047;--muted:#8e9bb0;--accent:#6d86ff;--green:#53d49a;--yellow:#f2c96d;--red:#ff7887;--cyan:#76cfff;--purple:#a7a5ff}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 14% -10%,#1d2944 0,transparent 34%),radial-gradient(circle at 95% 0,#16243a 0,transparent 28%),var(--bg)}button,input,select,textarea{font:inherit}.shell{max-width:1500px;margin:auto;padding:22px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:16px}.brand{font-size:25px;font-weight:850;letter-spacing:-.5px}.sub{font-size:12px;color:var(--muted);margin-top:3px}.top-actions{display:flex;gap:8px;align-items:center}.pill{display:inline-flex;align-items:center;gap:6px;padding:6px 9px;border:1px solid var(--border);background:#0c111a;border-radius:999px;font-size:12px;color:#c8d3e5}.dot{width:8px;height:8px;border-radius:50%;display:inline-block}.ok{background:var(--green)}.bad{background:var(--red)}.warn{background:var(--yellow)}select,input,textarea,button{border:1px solid #2d3952;background:#0a0f18;color:#edf2fb;border-radius:10px;padding:9px 11px}button{cursor:pointer;font-weight:700}button.primary{background:linear-gradient(135deg,#617cff,#7a64f3);border:0}button.secondary{background:#182235}button.ghost{background:transparent}button.danger{background:#351722;color:#ffc0c7;border-color:#5c2635}button:disabled{opacity:.45;cursor:not-allowed}.toolbar{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:center;margin-bottom:14px}.toolbar .left{display:flex;gap:8px;align-items:center;min-width:0}.toolbar select{max-width:380px}.stats{display:grid;grid-template-columns:repeat(5,minmax(120px,1fr));gap:10px;margin-bottom:14px}.stat{background:linear-gradient(180deg,rgba(17,24,36,.95),rgba(11,16,25,.95));border:1px solid var(--border);border-radius:14px;padding:13px}.stat .k{font-size:11px;text-transform:uppercase;letter-spacing:.8px;color:var(--muted)}.stat .v{font-size:23px;font-weight:850;margin-top:4px}.stat .hint{font-size:11px;color:var(--muted);margin-top:2px}.layout{display:grid;grid-template-columns:minmax(360px,.82fr) minmax(520px,1.38fr);gap:14px}.panel{background:rgba(13,18,28,.92);border:1px solid var(--border);border-radius:16px;padding:15px;box-shadow:0 20px 60px rgba(0,0,0,.22)}.panel h2{font-size:14px;margin:0 0 11px;display:flex;align-items:center;justify-content:space-between;gap:8px}.muted{color:var(--muted)}.small{font-size:11px}.composer textarea{width:100%;min-height:168px;resize:vertical;line-height:1.42}.composer .row{display:grid;grid-template-columns:1fr 1fr;gap:8px}.composer label{display:block;font-size:11px;color:var(--muted);margin:9px 0 5px}.composer .buttons{display:flex;gap:8px;margin-top:10px}.composer .buttons button{flex:1}.agents{display:grid;grid-template-columns:repeat(auto-fit,minmax(215px,1fr));gap:9px}.agent{border:1px solid #28344b;background:linear-gradient(180deg,#0d141f,#0a0f18);border-radius:13px;padding:11px;min-width:0}.agent-head{display:flex;align-items:center;justify-content:space-between;gap:8px}.agent-name{display:flex;align-items:center;gap:7px;font-weight:800}.badge{display:inline-flex;padding:3px 6px;border-radius:999px;background:#1b2638;border:1px solid #2d3a52;font-size:10px;color:#b8c5da}.agent-lines{margin-top:8px;display:grid;gap:3px;font-size:11px;word-break:break-word}.agent-actions{display:flex;gap:6px;margin-top:9px}.agent-actions button{padding:6px 8px;font-size:11px}.online{background:var(--green)}.offline{background:#657086}.tasks{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.task-col{background:#0a0f18;border:1px solid #222d41;border-radius:11px;padding:9px;min-height:80px}.task-col h3{font-size:11px;text-transform:uppercase;color:var(--muted);letter-spacing:.6px;margin:0 0 7px}.task{padding:7px;border:1px solid #253148;background:#101725;border-radius:8px;font-size:11px;margin-bottom:6px}.task:last-child{margin-bottom:0}.barriers{display:flex;flex-wrap:wrap;gap:7px}.barrier{padding:7px 9px;border-radius:10px;background:#111a28;border:1px solid #283650;font-size:11px}.commands-head{display:flex;align-items:center;gap:8px}.commands-head input{max-width:250px;padding:7px 9px}.command{border:1px solid #253149;background:#0a1019;border-radius:12px;padding:11px;margin-top:8px}.command:first-child{margin-top:0}.command-top{display:flex;justify-content:space-between;gap:10px}.command-title{font-weight:800;font-size:12px}.command-time{font-size:10px;color:var(--muted)}.command-prompt{font-size:11px;color:#c9d3e3;margin:7px 0;white-space:pre-wrap;max-height:86px;overflow:auto}.progress{height:6px;background:#1a2332;border-radius:999px;overflow:hidden;margin:7px 0}.progress>span{display:block;height:100%;background:linear-gradient(90deg,#617cff,#58d0a0)}.delivery-grid{display:flex;flex-wrap:wrap;gap:5px}.delivery{padding:3px 6px;border-radius:999px;background:#172131;font-size:10px;border:1px solid #283650}.queued{color:var(--yellow)}.delivered{color:var(--cyan)}.running{color:var(--purple)}.done{color:var(--green)}.failed{color:var(--red)}.command-actions{display:flex;gap:6px;margin-top:8px}.command-actions button{font-size:10px;padding:5px 7px}.activity{display:grid;gap:6px;max-height:260px;overflow:auto}.event{display:flex;gap:8px;align-items:flex-start;padding:7px 0;border-bottom:1px solid #1e2738;font-size:11px}.event:last-child{border-bottom:0}.event-time{color:var(--muted);min-width:62px}.empty{padding:18px;text-align:center;color:var(--muted);font-size:12px;border:1px dashed #2a354a;border-radius:10px}.toast{position:fixed;right:18px;bottom:18px;background:#151f30;border:1px solid #344663;padding:11px 14px;border-radius:10px;display:none;z-index:20;box-shadow:0 12px 40px rgba(0,0,0,.4)}.span2{grid-column:1/-1}.hintbox{margin-top:9px;padding:8px 10px;border-radius:9px;background:#0b1621;border:1px solid #1f3a52;color:#9bb8cf;font-size:10px}.kbd{padding:1px 5px;border:1px solid #3b465a;border-bottom-width:2px;border-radius:5px;background:#111722;font-size:10px}.rightstack{display:grid;gap:14px}@media(max-width:1100px){.layout{grid-template-columns:1fr}.stats{grid-template-columns:repeat(3,1fr)}}@media(max-width:720px){.shell{padding:12px}.topbar{align-items:flex-start;flex-direction:column}.stats{grid-template-columns:repeat(2,1fr)}.tasks{grid-template-columns:1fr 1fr}.composer .row{grid-template-columns:1fr}.toolbar{grid-template-columns:1fr}.toolbar .left{flex-wrap:wrap}.agents{grid-template-columns:1fr}}
</style>
</head>
<body><div class="shell">
<div class="topbar"><div><div class="brand">AntroSwarm Control Center <span class="badge">v0.3</span></div><div class="sub">One control plane for multi-agent ChatGPT/Codex swarms.</div></div><div class="top-actions"><span class="pill"><span id="connDot" class="dot warn"></span><span id="connText">connecting</span></span><span id="runnerPill" class="pill">runners: —</span><button class="ghost" id="refreshNow">Refresh</button></div></div>
<div class="toolbar"><div class="left"><select id="swarm"></select><span id="goal" class="muted small"></span></div><div class="pill"><span>Auto refresh</span><select id="refreshRate" style="width:auto;padding:4px 7px"><option value="1000">1s</option><option value="2000" selected>2s</option><option value="5000">5s</option><option value="0">off</option></select></div></div>
<div class="stats"><div class="stat"><div class="k">Agents</div><div class="v" id="sAgents">—</div><div class="hint" id="sAgentsHint">online / total</div></div><div class="stat"><div class="k">Tasks</div><div class="v" id="sTasks">—</div><div class="hint">done / total</div></div><div class="stat"><div class="k">Commands</div><div class="v" id="sCommands">—</div><div class="hint">active deliveries</div></div><div class="stat"><div class="k">Barriers</div><div class="v" id="sBarriers">—</div><div class="hint">released / total</div></div><div class="stat"><div class="k">Runners</div><div class="v" id="sRunners">—</div><div class="hint">connected</div></div></div>
<div class="layout">
<div class="leftstack">
<section class="panel composer"><h2><span>Command composer</span><span class="badge">broadcast / direct</span></h2><div class="row"><div><label>Target</label><select id="target"></select></div><div><label>Label</label><input id="label" placeholder="phase / milestone / task"/></div></div><label>Prompt</label><textarea id="prompt" placeholder="Tell the swarm what to achieve. For autonomous work, state the goal and constraints and let agents coordinate roles themselves."></textarea><div class="buttons"><button class="primary" id="send">Send command</button><button class="secondary" id="copyJoin">Copy join prompt</button><button class="secondary" id="copyWorker">Copy worker prompt</button></div><div class="hintbox">Ordinary ChatGPT tabs cannot be externally awakened after a turn has ended. Worker mode polls while a turn remains active; durable commands survive disconnects and are re-delivered after lease expiry.</div></section>
<section class="panel" style="margin-top:14px"><h2><span>Tasks</span><span id="taskCount" class="badge">0</span></h2><div id="tasks" class="tasks"></div></section>
<section class="panel" style="margin-top:14px"><h2>Barriers</h2><div id="barriers" class="barriers"></div></section>
</div>
<div class="rightstack">
<section class="panel"><h2><span>Agents</span><span id="agentCount" class="badge">0</span></h2><div id="agents" class="agents"></div></section>
<section class="panel"><div class="commands-head"><h2 style="flex:1;margin:0">Command queue</h2><input id="commandFilter" placeholder="filter commands"/></div><div id="commands" style="margin-top:10px"></div></section>
<section class="panel"><h2>Activity</h2><div id="activity" class="activity"></div></section>
</div></div>
</div><div id="toast" class="toast"></div>
<script>
const qs=new URLSearchParams(location.search);const token=qs.get('token')||'';const api=(p)=>p+(token?(p.includes('?')?'&':'?')+'token='+encodeURIComponent(token):'');let snapshot=null;let refreshTimer=null;let loading=false;let lastError='';
const $=id=>document.getElementById(id);function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}function toast(v){const e=$('toast');e.textContent=v;e.style.display='block';clearTimeout(e._t);e._t=setTimeout(()=>e.style.display='none',2400)}function currentSwarm(){return $('swarm').value}function agentName(s,id){const a=s.agents.find(x=>x.id===id);return a?a.displayName:id}function ago(ms){if(ms<1000)return 'now';if(ms<60000)return Math.round(ms/1000)+'s';if(ms<3600000)return Math.round(ms/60000)+'m';return Math.round(ms/3600000)+'h'}
async function load(){if(loading)return;loading=true;try{const selected=currentSwarm();const r=await fetch(api('/api/dashboard'),{cache:'no-store'});if(!r.ok)throw new Error(await r.text());snapshot=await r.json();const sw=$('swarm');sw.innerHTML=snapshot.swarms.map(s=>'<option value="'+esc(s.id)+'">'+esc(s.id)+'</option>').join('');if(selected&&snapshot.swarms.some(s=>s.id===selected))sw.value=selected;setConnection(true);render()}catch(e){lastError=e.message;setConnection(false)}finally{loading=false}}
function setConnection(ok){$('connDot').className='dot '+(ok?'ok':'bad');$('connText').textContent=ok?'connected':('offline'+(lastError?' · '+lastError.slice(0,60):''))}
function schedule(){clearInterval(refreshTimer);const ms=Number($('refreshRate').value);if(ms>0)refreshTimer=setInterval(load,ms)}
function render(){if(!snapshot)return;const s=snapshot.swarms.find(x=>x.id===currentSwarm())||snapshot.swarms[0];if(!s){$('goal').textContent='No swarms yet';return}const commands=snapshot.commands.filter(c=>c.swarmId===s.id);$('goal').textContent=(s.goal||'No goal')+(s.repo?' · '+s.repo:'');$('runnerPill').textContent='runners: '+snapshot.runners.length;$('sRunners').textContent=snapshot.runners.length;$('sAgents').textContent=s.onlineAgents+'/'+s.agents.length;$('sAgentsHint').textContent='online / total'+(s.expectedAgents?' · expected '+s.expectedAgents:'');const doneTasks=s.tasks.filter(t=>t.status==='done').length;$('sTasks').textContent=doneTasks+'/'+s.tasks.length;let activeDeliveries=0;commands.forEach(c=>Object.values(c.deliveries).forEach(d=>{if(d.status!=='done'&&d.status!=='failed')activeDeliveries++}));$('sCommands').textContent=activeDeliveries;const released=s.barriers.filter(b=>b.released).length;$('sBarriers').textContent=released+'/'+s.barriers.length;renderTarget(s);renderAgents(s);renderTasks(s);renderBarriers(s);renderCommands(s,commands);renderActivity(s,commands)}
function renderTarget(s){const target=$('target');const prev=target.value;target.innerHTML='<option value="all">All agents ('+s.agents.length+')</option><option value="online">Online agents ('+s.onlineAgents+')</option>'+s.agents.map(a=>'<option value="'+esc(a.id)+'">'+esc(a.displayName)+' · '+(a.online?'online':'offline')+'</option>').join('');if([...target.options].some(o=>o.value===prev))target.value=prev}
function renderAgents(s){$('agentCount').textContent=s.onlineAgents+'/'+s.agents.length;$('agents').innerHTML=s.agents.length?s.agents.map(a=>'<div class="agent"><div class="agent-head"><div class="agent-name"><span class="dot '+(a.online?'online':'offline')+'"></span>'+esc(a.displayName)+'</div><span class="badge">'+(a.reconnectSafe?'safe join':'legacy join')+'</span></div><div class="agent-lines"><div class="muted">'+esc(a.id)+'</div><div>task: '+esc(a.currentTaskId||'—')+'</div><div>branch: '+esc(a.branch||'—')+'</div><div class="muted">seen '+ago(a.ageMs)+' ago</div></div><div class="agent-actions"><button class="secondary" onclick="targetAgent(\''+esc(a.id)+'\')">Target</button><button class="ghost" onclick="copyText(\''+esc(a.id)+'\')">Copy ID</button></div></div>').join(''):'<div class="empty">No agents connected.</div>'}
function renderTasks(s){$('taskCount').textContent=s.tasks.length;const states=[['open','Open'],['in_progress','In progress'],['blocked','Blocked'],['done','Done']];$('tasks').innerHTML=states.map(([key,label])=>{const items=s.tasks.filter(t=>t.status===key);return '<div class="task-col"><h3>'+label+' · '+items.length+'</h3>'+(items.length?items.map(t=>'<div class="task"><b>'+esc(t.title)+'</b><div class="muted">'+esc(t.id)+'</div>'+(t.claimedBy?'<div>'+esc(agentName(s,t.claimedBy))+'</div>':'')+'</div>').join(''):'<div class="muted small">—</div>')+'</div>'}).join('')}
function renderBarriers(s){$('barriers').innerHTML=s.barriers.length?s.barriers.slice().reverse().map(b=>'<div class="barrier"><b>'+esc(b.stage)+'</b> · '+b.participants.length+'/'+b.expected+' · <span class="'+(b.released?'done':'queued')+'">'+(b.released?'released':'waiting')+'</span></div>').join(''):'<div class="empty">No barriers yet.</div>'}
function renderCommands(s,commands){const q=$('commandFilter').value.trim().toLowerCase();let list=commands.slice().reverse();if(q)list=list.filter(c=>(c.label||'').toLowerCase().includes(q)||c.prompt.toLowerCase().includes(q)||c.id.toLowerCase().includes(q));$('commands').innerHTML=list.length?list.map(c=>{const ds=Object.entries(c.deliveries);const done=ds.filter(([,d])=>d.status==='done').length;const failed=ds.filter(([,d])=>d.status==='failed').length;const pct=ds.length?Math.round(((done+failed)/ds.length)*100):0;const active=ds.some(([,d])=>d.status!=='done'&&d.status!=='failed');return '<div class="command"><div class="command-top"><div class="command-title">'+esc(c.label||c.id)+'</div><div class="command-time">'+new Date(c.createdAt).toLocaleTimeString()+'</div></div><div class="command-prompt">'+esc(c.prompt)+'</div><div class="progress"><span style="width:'+pct+'%"></span></div><div class="delivery-grid">'+ds.map(([id,d])=>'<span class="delivery '+esc(d.status)+'" title="attempts '+(d.attempts||0)+'">'+esc(agentName(s,id))+': '+esc(d.status)+(d.attempts>1?' ×'+d.attempts:'')+'</span>').join('')+'</div><div class="command-actions">'+(active?'<button class="danger" onclick="cancelCommand(\''+esc(c.id)+'\')">Cancel</button>':'')+(failed?'<button class="secondary" onclick="retryCommand(\''+esc(c.id)+'\')">Retry failed</button>':'')+'<button class="ghost" onclick="copyText(\''+esc(c.id)+'\')">Copy ID</button></div></div>'}).join(''):'<div class="empty">No matching commands.</div>'}
function renderActivity(s,commands){const ev=[];s.tasks.forEach(t=>ev.push({t:0,label:'Task '+t.status+': '+t.title}));s.barriers.forEach(b=>ev.push({t:b.updatedAt||0,label:'Barrier '+b.stage+': '+b.participants.length+'/'+b.expected+(b.released?' released':' waiting')}));commands.forEach(c=>{const states=Object.values(c.deliveries);ev.push({t:c.createdAt,label:'Command '+(c.label||c.id)+' · '+states.map(x=>x.status).join(', ')})});ev.sort((a,b)=>b.t-a.t);$('activity').innerHTML=ev.length?ev.slice(0,30).map(e=>'<div class="event"><div class="event-time">'+(e.t?new Date(e.t).toLocaleTimeString():'task')+'</div><div>'+esc(e.label)+'</div></div>').join(''):'<div class="empty">No activity yet.</div>'}
function targetAgent(id){$('target').value=id;$('prompt').focus()}async function copyText(v){await navigator.clipboard.writeText(v);toast('Copied')}async function send(){const prompt=$('prompt').value.trim();if(!prompt){toast('Prompt is empty');return}const requestId=(crypto.randomUUID?crypto.randomUUID():Date.now()+'-'+Math.random());const body={swarm_id:currentSwarm(),target:$('target').value,label:$('label').value,prompt,request_id:requestId};$('send').disabled=true;try{const r=await fetch(api('/api/commands'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());$('prompt').value='';toast('Command queued');await load()}catch(e){toast(e.message)}finally{$('send').disabled=false}}
async function cancelCommand(id){if(!confirm('Cancel unfinished deliveries for '+id+'?'))return;const r=await fetch(api('/api/commands/'+encodeURIComponent(id)+'/cancel'),{method:'POST'});if(!r.ok){toast(await r.text());return}toast('Cancelled');await load()}async function retryCommand(id){const r=await fetch(api('/api/commands/'+encodeURIComponent(id)+'/retry'),{method:'POST',headers:{'content-type':'application/json'},body:'{}'});if(!r.ok){toast(await r.text());return}toast('Failed deliveries requeued');await load()}
async function copyJoin(){const s=snapshot.swarms.find(x=>x.id===currentSwarm());const expected=(s&&s.expectedAgents)||10;const text='Use AntroSwarm Test. This is a NEW chat. Generate a random UUID-like join_key once and keep it for retries. Call swarm_join with swarm_id='+currentSwarm()+', expected_agents='+expected+', join_key=<your unique key>. Do not reuse another chat agent_id. Then call swarm_sync. Do not start implementation until the requested number of agents are connected. Never push unless the operator explicitly asks.';await navigator.clipboard.writeText(text);toast('Join prompt copied')}
async function copyWorker(){const text='Use AntroSwarm Test. Keep your existing identity in swarm '+currentSwarm()+'. Enter worker mode. Repeatedly call swarm_wait with wait_ms=20000. When a command arrives: swarm_ack, execute it, call swarm_command_heartbeat periodically during long work, then swarm_report done/failed with a concise result. Immediately return to swarm_wait while the current turn remains active. Never call swarm_join unless your identity is actually missing.';await navigator.clipboard.writeText(text);toast('Worker prompt copied')}
$('swarm').addEventListener('change',render);$('refreshNow').addEventListener('click',load);$('refreshRate').addEventListener('change',schedule);$('send').addEventListener('click',send);$('copyJoin').addEventListener('click',copyJoin);$('copyWorker').addEventListener('click',copyWorker);$('commandFilter').addEventListener('input',render);$('prompt').addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter')send()});schedule();load();
</script></body></html>`;
}
